import { createStore } from 'vuex'
import ordersList from './modules/ordersList'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    ordersList
  }
})
